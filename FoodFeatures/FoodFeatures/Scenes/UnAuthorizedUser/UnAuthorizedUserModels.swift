import UIKit

enum UnAuthorizedUser {
    enum RouteToAuth {
        struct Request {
            let navigationController: UINavigationController
        }
        struct Response {
            let navigationController: UINavigationController
        }
    }
}

